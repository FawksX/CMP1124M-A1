namespace SearchAndSort; 

/**
 * <summary>
 * A simple enum to determine if a <see cref="DataResult"/> is ordered in Ascending or Descending order
 * </summary>
 */
public enum OrderState {
    Ascending,
    Descending
}